class HinhTron{
    radius; color;
    constructor(radius, color) {
        this.radius = radius;
        this.color= color;
    }
    tinhDienTich(){
        let dientich = Math.PI * this.radius * this.radius;
    }
    tinhchuvi() {
        let chuvi = this.radius
    }
}
let